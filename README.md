## How to run it:

- npm install
- npm update
- npm start

##dependencies: 
    react: 16.12.0,
    react-dom: 16.12.0,
    react-scripts: 3.2.0,
    react-select: 3.0.8,

##About app:
Modal component for requesting an exported report. Function as described in "Technical section".
Styled as demanded in task. 
Generate button and result Modal not styled, because not mentioned in task.